<%= simple_form_for jersey do |c| %>
  <%= c.input :name %>
  <%= c.input :size %>
  <%= c.input :price %>
  <%= c.input :photo, as: :file %>
  <%= c.input :description %>
  <%= c.submit "#{jersey.id ? "Edit" : "Add" } Jersey", class: "btn btn-success" %>
<% end %>
